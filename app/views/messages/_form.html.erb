<%= turbo_frame_tag "#{dom_id(conversation)}_message_form" do %>
  <%= form_with(model: Message.new, url: [conversation, conversation.messages.new]) do |form| %>
    <div class="bg-gray-300 p-4">
      <%= form.text_field :content, rows: 4, class: "flex items-center h-10 w-full rounded px-3 text-sm", autofocus: true, placeholder: "Type your message…", "x-on:keydown.cmd.enter" => "$event.target.form.requestSubmit();" %>
    </div>
  <% end %>
<% end %>
